import PubSub from 'pubsub-js';

// Dispatch event
// PubSub.publish(KEY, data|null);

// Listener event
// useEffect(() => {
//   PubSub.subscribe(KEY_UPDATE_DELETED_ORDERGROUP, handleFunction);
// }, []);
